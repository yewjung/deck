apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-decks-deployment
  namespace: dev
spec:
  selector:
    matchLabels:
      app: redis-decks
  template:
    metadata:
      labels:
        app: redis-decks
    spec:
      containers:
      - name: redis-decks
        image: redis
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-decks-service
  namespace: dev
spec:
  selector:
    app: redis-decks
  ports:
  - port: 6379
    targetPort: 6379

